.DS_Store
node_modules/
*.log
# Old secrets location — the server now reads from ~/.msc-editor/secrets.json instead
# (outside the repo entirely, since a credential inside the served web root is reachable
# by construction). Keeping this ignored costs nothing and protects anyone upgrading from
# an earlier checkout that still has the old file sitting here.
editor/secrets.json
